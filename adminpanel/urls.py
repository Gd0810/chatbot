from django.urls import path
from . import views
from django.contrib import admin

urlpatterns = [
    # path('', admin.site.urls),
    # path('dashboard/', views.dashboard, name='dashboard'),
    # path('approve/<int:ws_id>/', views.approve_workspace, name='approve'),
] # Add views later